
services:
  db:
    image: postgres:16-alpine
    restart: "no"
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-legal_crm}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - backend_net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: "no"
    command: >
      sh -c "python manage.py migrate &&
             python manage.py collectstatic --noinput &&
             python manage.py seed_demo --skip-if-exists &&
             daphne -b 0.0.0.0 -p 8000 config.asgi:application"
    volumes:
      - ./backend:/app
      - media_files:/app/media
      - static_files:/app/staticfiles
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.demo
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - backend_net
      - frontend_net

  scheduler:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: "no"
    # send_deadline_reminders идемпотентна (дедуп по Notification.key),
    # поэтому часовой цикл безопасен
    command: sh -c 'while true; do python manage.py send_deadline_reminders; sleep 3600; done'
    volumes:
      - ./backend:/app
    env_file: .env
    environment:
      DJANGO_SETTINGS_MODULE: config.settings.demo
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - backend_net

  redis:
    image: redis:7-alpine
    restart: "no"
    networks:
      - backend_net

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        # пусто = относительные URL: всё (API, WS, static) ходит через nginx
        # того же origin. Намеренно не берём значение из .env (там адрес для
        # dev-режима с отдельным backend на :8000).
        VITE_API_BASE_URL: ""
    restart: "no"
    networks:
      - frontend_net

  nginx:
    image: nginx:1.25-alpine
    restart: "no"
    ports:
      - "80:80"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
      - static_files:/app/staticfiles:ro
    depends_on:
      - backend
      - frontend
    networks:
      - frontend_net

volumes:
  postgres_data:
  media_files:
  static_files:

networks:
  backend_net:
  frontend_net:
